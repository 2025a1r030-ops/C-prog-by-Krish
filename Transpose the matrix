#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {  
    int a[100][100], t[100][100]; 
    int r, c; 
    int i, j; 
    
    //Input rows and coloumns 
    scanf("%d %d", &r, &c); 
    
    //Input matrix elements 
    for(i=0; i<r; i++) { 
        for(j=0; j<c; j++) { 
            scanf("%d", &a[i][j]);
        }
    } 
    
    //Transpose (swap rows and coloums) 
    for(i=0; i<r; i++) { 
        for(j=0; j<c; j++) { 
             t[j][i] = a[i][j];
        } 
    }
        
        //Print Transpose 
        for(i=0; i<c; i++) { 
        for(j=0; j<r; j++) { 
            printf("%d ", t[i][j]);
        }  
            printf("\n"); 
        }
            
        

    
    return 0;
}
