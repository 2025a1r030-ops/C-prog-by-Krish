#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {   
    int A, B, hcf; 
    scanf("%d %d", &A, &B); 
    int i = 1; 
    hcf = 1;
    while(i<=A && i<=B) { 
        if(A%i == 0 && B%i == 0) { 
            hcf = i;
        } 
        i++;
    }  
    printf("%d", hcf);
       
    
    return 0;
}
