#include <stdio.h> 
#include <string.h> 

int main(){  
    //strcpy() 
    char oldstr[100] = "Krish"; 
    char newstr[100] = "Peer"; 
    strcpy(oldstr, newstr); 
    puts(oldstr); 
     return 0;
    }
