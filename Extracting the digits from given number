#include <stdio.h> 
#include <limits.h> 
int main() { 
    int num; 
    printf("Enter the Number : "); 
    scanf("%d", &num); 
    while(num != 0) { 
        int rem = num%10; 
        printf("%d", rem); 
        num = num/10; 
    }
    
return 0; 
} 
