#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {  
    int N, i=1; 
    scanf("%d", &N); 
    while(i <= N) { 
        if(N%i == 0) { 
            printf("%d ", i);
        } 
        i++;
    }

      
    return 0;
}
