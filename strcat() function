#include <stdio.h> 
#include <string.h> 

int main(){  
    //strcat() 
    char oldstr[100] = "Satyam loves "; 
    char newstr[100] = "Reality"; 
    strcat(oldstr, newstr); 
    puts(oldstr); 
     return 0;
    }
